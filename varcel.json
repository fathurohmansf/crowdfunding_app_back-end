{
    "version": 2,
    "builds": [{
        "GO_BUILD_FLAGS": "-ldflags '-s -w'",
        "src": "main.go",
        "use": "@vercel/go"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/api"
    }]
}